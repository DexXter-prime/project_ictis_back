<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
     rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
     crossorigin="anonymous">

    <title>Билиотека проектов</title>
</head>
<body>
    <div class='container'>
        <h1 class='mb-4'>Билиотека проектов</h1>
        <a href='/articles/new' class='btn btn-success mt-3 mb-4'>Добавить новый проект</a>
        <%articles.forEach(article => { %>
            <div class='card mb-4'>
                <div class='card-body'>
                    <div class='card-title'>
                        <%= article.title %>
                    </div>
                    <div class='card-subtitle text-muted'>
                        <%= article.currentDate.toLocaleDateString()%>
                    </div>
                    <div class='card-text'>
                        <%= article.description%>
                    </div>
                    <a href='articles/<%= article.slug %>' class='btn btn-primary mt-4 mb-4'>Посмотреть описание</a>
                    <form action='articles/<%= article.id%>?_method=DELETE' method='POST' class='d-inline'>
                        <button type='submit' class='btn btn-danger' >Удалить</button>
                    </form>
                    <a href = 'articles/edit/<%= article.id%>' class='btn btn-secondary'>Изменить</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>